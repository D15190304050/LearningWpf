<Window x:Class="AdvancedDataBinding.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AdvancedDataBinding"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="300">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="5" />
            <Setter Property="Margin" Value="5" />
        </Style>
    </Window.Resources>
    
    <Grid>
        <StackPanel Button.Click="ButtonClick">
            <Button>ValueConverter</Button>
            <Button>MoreValueConverter</Button>
            <Button>ListStyles</Button>
            <Button>RadioButtonListStyle</Button>
            <Button>CheckBoxList</Button>
            <Button>VariedStyles</Button>
        </StackPanel>
    </Grid>
</Window>
